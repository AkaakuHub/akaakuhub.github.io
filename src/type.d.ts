export type modalArrayPropsType = {
  modalArray: string[];
  setModalArray: React.Dispatch<React.SetStateAction<string[]>>;
};


export type SVGPropsType = {
  style?: CSSProperties;
  color?: string;
};